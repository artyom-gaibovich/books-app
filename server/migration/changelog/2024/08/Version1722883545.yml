databaseChangeLog:
  - changeSet:
      id: version-1722883545
      author: your_name
      changes:
        - sql:
            sql:
              CREATE UNIQUE INDEX idx_users_email ON "user".users(email);

